# No-op — the devcontainer Dockerfile handles all setup.
# This runs once at codespace creation as the dotfiles entry point.
echo ">>> install.sh ($(date '+%H:%M:%S')) — no-op, devcontainer handles setup"
